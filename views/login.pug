extends ./includes/layout.pug
block content
    form(method="post")
        | name：
        input(name="name")
        br
        | pwd：
        input(nane="pwd")
        br
        input(type='submit' value='submit') 
