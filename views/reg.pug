extends ./includes/layout.pug
block content
    form(method="post")
        | name：
        input(name="name")
        br
        | pwd：
        input(nane="pwd")
        br
        | confirm：
        input(nane="confirm")
        br
        | email：
        input(nane="email")
        br    
        input(type='submit' value='submit') 
